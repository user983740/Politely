# Swagger disabled in production
springdoc.swagger-ui.enabled=false
springdoc.api-docs.enabled=false

# Resend
resend.api-key=${RESEND_API_KEY}
app.email.sender=${APP_EMAIL_SENDER:noreply@politely-ai.com}
